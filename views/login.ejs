<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>myi - Login</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        /* Bronze Color Palette */
        :root {
            --acajou: #5F3D1F;
            --old-burgundy: #372C2E;
            --white: #FFFFFF;
            --indian-yellow: #CB984F;
            --russet: #7A431D;
            --dark-bg: #2A1F1C;
            --light-bg: #3D2E28;
            --bronze-dark: #563727;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'IBM Plex Mono', monospace;
            background: var(--old-burgundy);
            color: var(--white);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        /* Abstract line patterns */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                radial-gradient(ellipse at 20% 80%, rgba(203, 152, 79, 0.08) 0%, transparent 50%),
                radial-gradient(ellipse at 80% 20%, rgba(203, 152, 79, 0.06) 0%, transparent 50%);
            pointer-events: none;
            z-index: 0;
        }

        .abstract-lines {
            position: fixed;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            pointer-events: none;
            z-index: 1;
            overflow: hidden;
        }

        .line-pattern {
            position: absolute;
            border: 1px solid rgba(203, 152, 79, 0.15);
            border-radius: 50%;
        }

        .line-pattern-1 {
            width: 800px;
            height: 800px;
            top: -400px;
            left: -200px;
            border-top: none;
            border-right: none;
        }

        .line-pattern-2 {
            width: 600px;
            height: 600px;
            bottom: -300px;
            right: -150px;
            border-bottom: none;
            border-left: none;
        }

        .line-pattern-3 {
            width: 1000px;
            height: 1000px;
            top: 50%;
            right: -500px;
            border-top: none;
            border-left: none;
            transform: rotate(45deg);
        }

        .login-container {
            text-align: center;
            z-index: 10;
            padding: 60px;
            background: linear-gradient(135deg, var(--bronze-dark) 0%, var(--old-burgundy) 100%);
            border: 1px solid rgba(203, 152, 79, 0.25);
            border-radius: 30px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.6);
            max-width: 500px;
            width: 90%;
        }

        .brand-logo {
            font-family: 'IBM Plex Mono', monospace;
            font-size: 7em;
            font-weight: 700;
            letter-spacing: 4px;
            color: var(--white);
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.5);
            margin-bottom: 10px;
        }

        .brand-tagline {
            font-family: 'IBM Plex Mono', monospace;
            font-size: 1.2em;
            color: rgba(255, 255, 255, 0.7);
            letter-spacing: 3px;
            font-weight: 300;
            margin-bottom: 50px;
        }

        .login-description {
            font-size: 1.05em;
            line-height: 1.8;
            color: rgba(255, 255, 255, 0.75);
            margin-bottom: 40px;
            font-weight: 300;
        }

        .spotify-login-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            background: linear-gradient(135deg, #1DB954 0%, #1aa34a 100%);
            color: var(--white);
            padding: 18px 40px;
            border: none;
            border-radius: 50px;
            font-size: 1.1em;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            text-decoration: none;
            box-shadow: 0 8px 25px rgba(29, 185, 84, 0.35);
            font-family: 'IBM Plex Mono', monospace;
        }

        .spotify-login-btn:hover {
            transform: translateY(-4px) scale(1.02);
            box-shadow: 0 12px 35px rgba(29, 185, 84, 0.5);
        }

        .spotify-login-btn:active {
            transform: translateY(-2px) scale(1.01);
        }

        .spotify-icon {
            width: 24px;
            height: 24px;
        }

        .features-preview {
            margin-top: 50px;
            padding-top: 40px;
            border-top: 1px solid rgba(203, 152, 79, 0.2);
        }

        .features-title {
            font-size: 0.9em;
            color: rgba(255, 255, 255, 0.5);
            letter-spacing: 2px;
            text-transform: uppercase;
            margin-bottom: 20px;
        }

        .features-list {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 15px;
        }

        .feature-tag {
            background: rgba(203, 152, 79, 0.15);
            border: 1px solid rgba(203, 152, 79, 0.25);
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 0.85em;
            color: var(--indian-yellow);
            font-weight: 500;
        }

        @media (max-width: 768px) {
            .brand-logo {
                font-size: 5em;
            }

            .login-container {
                padding: 40px 30px;
            }

            .spotify-login-btn {
                padding: 16px 32px;
                font-size: 1em;
            }
        }
    </style>
</head>
<body>
    <div class="abstract-lines">
        <div class="line-pattern line-pattern-1"></div>
        <div class="line-pattern line-pattern-2"></div>
        <div class="line-pattern line-pattern-3"></div>
    </div>

    <div class="login-container">
        <div class="brand-logo">myi</div>
        <div class="brand-tagline">Music Insights</div>
        
        <p class="login-description">
            Discover your listening patterns, explore your top tracks and artists, 
            and create personalized playlists from your music library.
        </p>

        <a href="/auth/spotify" class="spotify-login-btn">
            <svg class="spotify-icon" viewBox="0 0 24 24" fill="currentColor">
                <path d="M12 0C5.4 0 0 5.4 0 12s5.4 12 12 12 12-5.4 12-12S18.66 0 12 0zm5.521 17.34c-.24.359-.66.48-1.021.24-2.82-1.74-6.36-2.101-10.561-1.141-.418.122-.779-.179-.899-.539-.12-.421.18-.78.54-.9 4.56-1.021 8.52-.6 11.64 1.32.42.18.479.659.301 1.02zm1.44-3.3c-.301.42-.841.6-1.262.3-3.239-1.98-8.159-2.58-11.939-1.38-.479.12-1.02-.12-1.14-.6-.12-.48.12-1.021.6-1.141C9.6 9.9 15 10.561 18.72 12.84c.361.181.54.78.241 1.2zm.12-3.36C15.24 8.4 8.82 8.16 5.16 9.301c-.6.179-1.2-.181-1.38-.721-.18-.601.18-1.2.72-1.381 4.26-1.26 11.28-1.02 15.721 1.621.539.3.719 1.02.419 1.56-.299.421-1.02.599-1.559.3z"/>
            </svg>
            Login with Spotify
        </a>

        <div class="features-preview">
            <div class="features-title">What you'll discover</div>
            <div class="features-list">
                <span class="feature-tag">Top Tracks</span>
                <span class="feature-tag">Top Artists</span>
                <span class="feature-tag">Listening Insights</span>
                <span class="feature-tag">Artist Deep Dive</span>
                <span class="feature-tag">Mood Playlists</span>
            </div>
        </div>
    </div>
</body>
</html>

